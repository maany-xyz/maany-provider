package types

// Base denom & display denom for the Maany provider chain.
const (
    BaseDenom     = "umaany" // <- your base denom
    DisplayDenom  = "maany"  // <- display denom (6 exp typical)
    DefaultGasStr = "0.005"  // default gas price amount (string)
)